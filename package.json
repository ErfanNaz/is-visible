{
  "name": "is-visible-directive",
  "version": "1.0.0",
  "description": "With this lib, it is easy to add or remove classes from HTML by adding additional attributes to the HTML elements",
  "main": "dist/index.js",
  "source": "src/index.ts",
  "module": "dist/module.js",
  "types": "dist/types.d.ts",
  "scripts": {
    "watch": "parcel watch",
    "build": "parcel build",
    "start:example": "parcel ./src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["HTML","CSS","No-Code","directive"],
  "author": "Erfan Nazmehr",
  "license": "ISC",
  "targets": {
    "main": {
      "optimize": true
    }
  },
  "devDependencies": {
    "@parcel/packager-ts": "^2.6.2",
    "@parcel/transformer-typescript-types": "^2.6.2",
    "parcel": "^2.6.2",
    "typescript": "^4.7.4"
  }
}
